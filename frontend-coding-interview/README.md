
## Getting Started

```bash
npm run dev
```

## Things I would do if I had more time
- Fix styling, I couldn't get all the styling to match the figma and it was also difficult to match the styling on Figma w/o dev mode
- Make authenication logic better maybe using something like ClerkJs
- pagination or load more for photos
- error and loading handling using Suspense and Error Boundaries
- better security practices definetly doing something to hide the API key
- better testing including E2E tests using something like Cypress or Playwright, didn't really configure the jest tests that well b/c of time contraints
- Talk w/ designer on making site UI/UX like better animations
- Actually persisting like/unlike state
- figuring out good config for tanstack quesry for when data is stale/when to refetch
- Implement a forgot password
